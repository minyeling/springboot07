<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="school.minyeling.dao.SafeMapper">

    <insert id="insertOne" parameterType="school.minyeling.domain.Safe">
        insert into safe values(#{FilterName},#{RemoteHost},#{RemoteAddr});
    </insert>

</mapper>